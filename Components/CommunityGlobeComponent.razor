@using Microsoft.AspNetCore.Components
@using ZealousMindedPeopleGeo.Models
@using ZealousMindedPeopleGeo.Services.Mapping
@using ZealousMindedPeopleGeo.Services.Repositories
@implements IDisposable


<HeadContent>
    <link href="/_content/ZealousMindedPeopleGeo/css/community-globe.css" rel="stylesheet" />
</HeadContent>

<div class="d-flex flex-column align-items-center gap-3">
    <div id="@ContainerId" class="globe-canvas-container"
         style="width: @(Width)px; height: @(Height)px;">

        @if (_isInitializing)
        {
            <div class="globe-loading-overlay text-white text-center">
                <div class="loading-spinner mb-3"></div>
                <div>Загрузка глобуса</div>
            </div>
        }

        @if (!string.IsNullOrEmpty(_errorMessage))
        {
            <div class="globe-error-overlay text-center p-4">
                <div class="fs-1 mb-3">⚠️</div>
                <div class="text-danger mb-3">@_errorMessage</div>
                <button class="btn btn-primary" @onclick="RetryInitializationAsync">
                    Повторить
                </button>
            </div>
        }
    </div>

    @if (_isInitialized && ShowControls)
    {
        <div class="globe-controls d-flex gap-3 align-items-center p-2 rounded text-white">
            <div class="d-flex align-items-center gap-2">
                <small>Вращение:</small>
                <button class="btn btn-sm @(_isAutoRotating ? "btn-info" : "btn-outline-light")"
                        @onclick="ToggleRotationAsync">
                    🔄
                </button>
            </div>

            <div class="d-flex align-items-center gap-2">
                <small>Участники:</small>
                <span class="badge bg-info">@_participantCount</span>
            </div>

            <div class="d-flex align-items-center gap-2">
                <small>Детализация:</small>
                <select class="form-select form-select-sm" @bind="_currentLod" style="width: auto;">
                    <option value="0">Низкая</option>
                    <option value="1">Средняя</option>
                    <option value="2">Высокая</option>
                    <option value="3">Ультра</option>
                </select>
            </div>

            <div class="d-flex gap-2">
                <button class="btn btn-sm btn-outline-light" @onclick="RefreshDataAsync" title="Обновить данные">
                    🔄
                </button>
                <button class="btn btn-sm btn-outline-light" @onclick="CenterOnUserAsync" title="Центрировать">
                    🎯
                </button>
            </div>
        </div>
    }

    <!-- Панель добавления участников -->
    @if (_isInitialized && ShowParticipantManagement)
    {
        <div class="participant-management-panel">
            <div class="panel-header">
                <h3>👤 Управление участниками</h3>
                <button class="panel-toggle" @onclick="ToggleParticipantPanel">
                    @(_isParticipantPanelCollapsed ? "▼" : "▲")
                </button>
            </div>

            @if (!_isParticipantPanelCollapsed)
            {
                <div class="panel-content">
                    <div class="participant-form">
                        <div class="form-row">
                            <div class="form-group">
                                <label>Имя:</label>
                                <InputText @bind-Value="_newParticipant.Name" placeholder="Введите имя участника" class="form-input" />
                            </div>
                            <div class="form-group">
                                <label>Широта:</label>
                                <InputNumber @bind-Value="_newParticipant.Latitude" placeholder="Например 55.7558" step="0.0001" class="form-input" />
                            </div>
                            <div class="form-group">
                                <label>Долгота:</label>
                                <InputNumber @bind-Value="_newParticipant.Longitude" placeholder="Например 37.6173" step="0.0001" class="form-input" />
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="button" class="btn btn-success" @onclick="AddParticipantAsync" disabled="@_isAddingParticipant">
                                @if (_isAddingParticipant) { <span>⏳ Добавление...</span> } else { <span>✅ Добавить участника</span> }
                            </button>
                            <button type="button" class="btn btn-secondary" @onclick="ClearParticipantForm">🗑️ Очистить</button>
                            <button type="button" class="btn btn-info" @onclick="CenterOnFormCoordinates">🎯 Центрировать</button>
                        </div>
                    </div>

                    <div class="border-top pt-3 mt-3">
                        <h6 class="text-white mb-2">⚡ Быстрое добавление:</h6>
                        <div class="d-flex flex-wrap gap-2">
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("Москва", 55.7558, 37.6173))">🏛️ Москва</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("СПб", 59.9343, 30.3351))">🏛️ СПб</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("Новосибирск", 55.0084, 82.9357))">🏛️ Новосибирск</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("Екатеринбург", 56.8389, 60.6057))">🏛️ Екатеринбург</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("Казань", 55.7961, 49.1064))">🏛️ Казань</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="@(() => AddQuickParticipantAsync("Краснодар", 45.0355, 38.9753))">🏛️ Краснодар</button>
                            <button class="btn btn-sm btn-outline-info" @onclick="() => AddRandomParticipantAsync()">🎲 Случайный</button>
                        </div>
                    </div>


                </div>
            }

            @if (!string.IsNullOrEmpty(_operationMessage))
            {
                <div class="alert @(string.IsNullOrEmpty(_operationError) ? "alert-success" : "alert-danger") mt-3">
                    @if (string.IsNullOrEmpty(_operationError)) { <span>✅ @_operationMessage</span> } else { <span>❌ @_operationMessage</span> }
                </div>
            }
        </div>
    }

    <!-- Информация об участниках -->
    @if (_isInitialized && ShowParticipantInfo && _selectedParticipant != null)
    {
        <div class="participant-info-panel">
            <div class="participant-info-header">
                <h4>@_selectedParticipant.Name</h4>
                <button class="close-button" @onclick="CloseParticipantInfo">×</button>
            </div>
            <div class="participant-info-content">
                <div class="participant-detail">
                    <strong>Местоположение:</strong>
                    <span>@_selectedParticipant.City, @_selectedParticipant.Country</span>
                </div>
                <div class="participant-detail">
                    <strong>Координаты:</strong>
                    <span>@(_selectedParticipant.Latitude?.ToString("F4") ?? "N/A"), @(_selectedParticipant.Longitude?.ToString("F4") ?? "N/A")</span>
                </div>
                @if (!string.IsNullOrEmpty(_selectedParticipant.Message))
                {
                    <div class="participant-detail">
                        <strong>Сообщение:</strong>
                        <span>@_selectedParticipant.Message</span>
                    </div>
                }
                <div class="participant-detail">
                    <strong>Зарегистрирован:</strong>
                    <span>@_selectedParticipant.RegisteredAt.ToString("g")</span>
                </div>
            </div>
        </div>
    }
</div>

